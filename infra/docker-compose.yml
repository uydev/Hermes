services:
  backend:
    build:
      context: ../backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
    # If you create backend/.env locally, uncomment:
    # env_file:
    #   - ../backend/.env
